---
"@moonshot-ai/kimi-code": minor
---

Add per-profile modelAlias and thinkingEffort configuration for subagents, enabling cost-optimized routing where subagents can use different models from the parent agent.
